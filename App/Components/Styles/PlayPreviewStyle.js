import { StyleSheet } from 'react-native'
import Colors from '../../Themes/Colors'

export default StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 50,
    paddingBottom: 50
  },
  label: {
    fontFamily: 'Cochin',
    textAlign: 'center',
    color: Colors.snow
  }
})
